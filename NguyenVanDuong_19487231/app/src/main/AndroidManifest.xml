<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testfirebase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NguyenVanDuong_19487231">
        <activity android:name=".update_sp"></activity>
        <activity android:name=".xoa_sp" />
        <activity android:name=".them_sp" />
        <activity android:name=".page_trang_chu" />
        <activity android:name=".register" />
        <activity android:name=".page_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>